// This file is required by karma.conf.js and loads recursively all the .spec and framework files

export const environment = {
  production: true,
  apiUrl: 'https://tu-dominio.com/api' // Cambiar a tu dominio en producción
};
